var countries = ["India", "USA", "UK", "Japan", "Sweden"];

var states = new Array("Kerala", "Karnataka", "TamilNadu", "Rajastan");

console.log(states[1]);

console.log(states.length); //.length on array return the length of the array

states.pop(); //.pop on array pops th last element in t he array

states.unshift("NEWVAL"); //.unshift on array adds a element to the begining of the list

states.shift(); //..shift on array removes the first element
